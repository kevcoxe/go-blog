version: "3.7"

services:
  blog_api:
    image: kevcoxe/go-blog-api
    container_name: go_blog_api
    restart: always
    environment:
      - PORT=5000
    expose:
      - 5000
    ports:
      - "5000:5000"

  # blog_api_local:
  #   build: ./api
  #   container_name: go_blog_api_local
  #   restart: always
  #   environment:
  #     - PORT=5000
  #   expose:
  #     - 5000
  #   ports:
  #     - "5000:5000"
